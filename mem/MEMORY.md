# Memory index — heart-of-the-saturn

- [Input and the endian sprite bug](2026-08-06-input-and-endian-sprite-bug.md) — session handoff at 94aee4b: CD-DA and input shipped and verified; a big-endian type-mismatch in the sprite free list fixed but not yet confirmed on hardware.
- [SFX silent in gameplay](2026-08-08-saturn-sfx-silent-in-gameplay.md) — RESOLVED at 21e24fb: the volume was mapped onto slPCMOn's dB attenuation as if linear. Kept for the five refuted hypotheses and the cycle-costing traps; the SCSP contention experiment is answered (it does not contend).
- [Death animation CD-DA sync](2026-08-11-death-animation-cdda-sync.md) — the delay is the fade, not disc_wait_for_music, which returns in 16 ms; four attempts to make it conditional each lost the splat. A two-segment death is two adjacent 0x21 opcodes, so its whole 960 ms is now spent in one fade up front. Read before touching play_death_animation.
- [Death fade shape and the history squash](2026-08-13-death-fade-shape-and-history-squash.md) — the 67-commit history exists only in the local branch pre-squash-845ffac; origin/main was force-pushed over it. Nothing in the squashed commit has run on hardware.
- [Backup RAM saves](2026-08-14-backup-ram-saves.md) — shipped and hardware-verified at a8e70b9, squashed; origin still holds the unsquashed 20 commits, so local and remote have diverged. Read before the save/load and pause menu spec: the src/ glob breaks the link if a caller lands before its callee, Digital's Button enum spells only START in caps, and deleting the debug chord can delete the proven save point.
- [Sub-title and save menus](2026-08-14-subtitle-and-save-menus.md) — shipped on port/subtitle-and-save-menus; only the cutscene fade-out is hardware-verified. All six requirements from the second emulator run are now implemented at ac41645 and none of them has been seen running. Read it before touching a fade: VDP1 sprite screens need their own fade layer because video_set_fade reaches only VDP2, a death never changes rooms so the room-7 gate could never see one, and the mid-game handoff to room 6 needs its own arm or the back half of the game runs black.
- [Boot sequence build](2026-08-13-boot-sequence-build.md) — boots and the menu works, on branch port/boot-sequence, unmerged. Four traps that all failed silently: the HWRAM heap was 366 bytes short of SDDRVS.TSK so SGL's sound driver initialised from the boot ROM; VDP1 sprite width must be a multiple of 8 or the sprite shears; the Kega AVI capture dropped a whole screen and baked one logo into another with 0 decode errors; and SRL::Bitmap::TGA is unusable here while TryLoadTexture refuses paletted bitmaps without a handler.
